package com.example.ticketing.auth;

public enum UserAuditAction {
    LOGIN,
    PASSWORD_CHANGED,
    PASSWORD_RESET,
    USER_CREATED,
    USER_DELETED,
    USER_DISABLED,
    USER_ENABLED,
    ROLE_CHANGED,
    PROFILE_UPDATED
}
